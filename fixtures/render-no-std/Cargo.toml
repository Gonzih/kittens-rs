[package]
name = "kittens-render-no-std-fixture"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
kittens = { path = "../../crates/kittens", default-features = false, features = ["macros"] }
kittens-render = { path = "../../crates/kittens-render", default-features = false }

[[bin]]
name = "kittens-render-no-std-fixture"
path = "src/main.rs"
test = false
bench = false
