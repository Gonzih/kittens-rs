[package]
name = "kittens-feature-unifier"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
kittens = { path = "../../crates/kittens", default-features = false, features = ["tokio"] }
