[package]
name = "kittens-code-protocol"
description = "Wire contract for the kittens-code harness family: ops, events, errors, policies, session configuration, and budget data"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
