# K2R-0 trace manifest

SPEC section 8 requires each suite row to map to named evidence and publish
its negative boundaries beside it. Accordingly, every row below names an
executable or compiling negative control, or says explicitly why that row has
none. The UI-pass suite and README publish compiling escape surfaces enforced
only by documentation. Exit-review finding 14 requires this manifest.
Statuses: ✓ (oracle in CI), **documentation** (prose contract with a compiling
control), **OPEN** (host work remaining), **GATED** (blocked on a named
external gate).

| SPEC §8 requirement | Status | Oracle(s) / gate | Adjacent negative control or honest absence |
|---|---|---|---|
| completion polled-then-lost | ✓ | `polled_then_lost_arbitration_gets_exactly_one_wake` | None: this is one required positive arbitration position; the registration-order falsifier below exercises a different defect. |
| completion unpolled-below-winner | ✓ | `unpolled_below_winner_recovers_on_first_poll` | None: this is the other required positive arbitration position, with no accepted bypass. |
| completion during waker registration | ✓ | `completion_during_registration_is_not_lost` | `negative_control_check_then_register_loses_the_wake` implements the broken check-then-register order and deterministically loses progress. |
| cancel-and-drain on every in-flight state | ✓ | `cancel_then_late_completion_stays_cancelled` (linearization), `drain_racing_prior_completion_reports_completed`, `spare_is_writable_during_flight_and_drain_flag_clears` | None: both sides of the cancel/completion race are required positive traces; ordinary drop is the separately named non-returning boundary in the next row. |
| ordinary-drop runtime oracle | ✓ | `dropped_pending_transfer_disarms_the_slot` (transfer), `abandon_recovers_a_dropped_sweep` (demand) | The returning contrast is explicit cancel-and-drain. Ordinary drop intentionally returns no resources, and no stronger guarantee is claimed. |
| resource recovery under injected failure | ✓ (model boundary) / **GATED** (enumerated command/chunk trace) | `failure_settles_returns_resources_and_mints_unwritten_witness`; the enumerated per-command/chunk trace belongs to the concrete transport integration — Xtensa/board gate | `tests/ui-pass/shared_sent_spare_backing.rs` shows that unconstrained sent-buffer and spare values may share safe interior-mutable backing; generic ownership does not prove physical disjointness. |
| waker replacement, late IRQ, slot reuse | ✓ | `waker_replacement_wakes_only_the_newest`, `late_completion_after_recovery_is_inert_via_disarm`, `sequential_transfers_reuse_the_same_slot` | None: these are lifecycle regression traces and there is no accepted stale-registration escape inside the carrier. |
| sweep coverage is a construction | ✓ | `starter_error_returns_target_spare_and_error_for_retry`, `only_one_target_is_outstanding_and_settlement_clears_it`, `cancelled_settlement_poisons_sweep`, `failed_settlement_poisons_sweep`, `cross_demand_stripe_settlement_is_rejected_without_mutation`, `plan_tiles_the_panel_exactly_including_partial_last_stripe`, `invalid_plans_are_rejected_including_overflow`; `tests/ui/in_flight_new_is_not_public.rs`, `forge_stripe_written.rs`, `forge_stripe_unwritten.rs`, and the replay/clone controls enforce the public boundary | `tests/ui-pass/dishonest_starter_ignores_region.rs` publishes the remaining adapter-honesty obligation: structural pairing supplies the exact region but cannot prove an open integration wrote it. The outer `StripeSettlement` needs one of the two unforgeable inner witnesses, so those two forge failures cover its construction boundary. |
| full-frame vs stripe pixel equivalence (`FrameEpoch` reconstruction) | **OPEN** | Requires the draw-target integration layer (embedded-graphics stripe target); lands with that slice, not fabricated before it | None yet: no draw-target integration exists on which to construct a meaningful pixel-divergence control. |
| demand-policy state table | ✓ | One oracle per table row in `k2r0_demand_sweep.rs`: coalescing/monotonic epochs, one-machine-active-epoch, request-during-sweep, throttle/eligibility, failed-retains, invalidation-discards, effective-clears, abandon-recovers | `tests/ui-pass/old_sweep_survives_abandon.rs` publishes the retained-value/stale-write window that ordinary ownership cannot revoke; drain plus forced repaint and `invalidate()` are the remedies. |
| stale/foreign/duplicate settlement | ✓ | `foreign_and_stale_settlement_is_rejected_without_mutation`, `foreign_failed_settlement_is_rejected_without_mutation`, `stale_failed_settlement_is_rejected_without_mutation`, `cross_demand_stripe_settlement_is_rejected_without_mutation`, `abandoned_epochs_witnesses_are_terminally_rejected`; every external rejection compares observable state/progress before and after. The crate-private `settle_rejection_predicates_preserve_all_private_state` additionally isolates no-outstanding, foreign demand, stale epoch, wrong region, fully covered, and after-poison predicates and compares the entire private sweep state. | `tests/ui/replay_stripe_written.rs`, `replay_sweep_written.rs`, `clone_stripe_target.rs`, `clone_stripe_written.rs`, `clone_stripe_unwritten.rs`, `clone_stripe_settlement.rs`, `clone_sweep_written.rs`, `clone_aborted_sweep.rs`, and `clone_settled.rs` make safe duplicate use unrepresentable. Private witness construction in the white-box oracle is intentionally unavailable to external code, as the forge failures prove. |
| snapshot ownership through the sweep | ✓ | `snapshot_is_owned_through_the_sweep_and_returned_at_the_end` proves owned/shared-reference access for an ordinary value | `tests/ui-pass/interior_mutable_snapshot.rs` shows that logical deep immutability is a caller obligation. |
| touch interleavings (findings 10–13 set) | ✓ | `k2r0_touch.rs` (16 oracles): `increment_then_latch_closes_idle_check_lost_wake`, startup/failure/budget/wrap/coalescing traces, and the round-1 set | `negative_control_check_before_increment_loses_idle_wake` implements the broken ordering and exposes pending work with neither consumer nor wake. |
| Outcome-B receiver/task traces | not applicable | Mechanism C selected (`K2R0A-LOG.md`); B was not needed | None: Outcome B is outside the selected mechanism, so inventing receiver/task controls would claim an implementation that does not exist. |
| external-consumer seam fixture | **GATED** | Bilateral seam co-sign with the `kittens-code` workstream (SPEC section 10) | None until both workstreams co-sign the seam; the render-only fixture cannot serve as a foreign-reactor control. |
| target compile/link against the chosen HAL SHA | **GATED** | Xtensa toolchain approval (`probes/esp32s3-spi2/`) | `adapter-blueprint.rs` is explicitly a non-compiling pseudocode delta over `VERDICT.md`, not a target-build control. |
| blocking `write_region` exact-SHA/no-allocation adapter (SPEC §9) | **GATED** | Compile an actual adapter against the pinned display-driver/HAL SHA and demonstrate zero allocation after initialization; upstream-versus-fork decision remains open | The pseudocode delta and host model are explicit non-controls: neither compiles the blocking capability nor measures allocation against the pinned target stack. |
| real `kittens::reactor!` integration fixture | **GATED** | Kernel-admitted source carrier (K2R-0A open item 3; root SPEC 37.6 comparison) | The raw-`Context` host oracles compile and run but explicitly do not prove delivery through reactor admission. |
| slow-successful-sweep throttle | ✓ | `slow_sweep_throttles_from_its_finish_instant`, `regressing_finish_time_is_clamped` | None: this is a state-table rule; arbitrary in-range forward `Tick` values remain a documented trusted-time boundary. |
| request during active sweep | ✓ | `request_during_active_sweep_survives_settlement` | None: this is a required coalescing transition with no alternate accepted spelling or bypass. |
| demand-provenance and epoch exhaustion | ✓ | `demand_id_exhaustion_is_sticky`; `epoch_horizon_mints_max_once_then_panics_stickily` proves epoch `u64::MAX` is minted once and the next attempt panics before mutation with the same message in every build profile | The caught-panic retry in each oracle is the negative control: exhaustion must not wrap, reopen, or mutate the machine. |
| eligibility arithmetic at `Tick::MAX` | ✓ | `tick_horizon_is_checked_without_state_mutation` proves overflow panics before mutation instead of shortening the minimum interval through saturation | The caught overflow and before/after state comparison are the negative control for the former saturating-arithmetic behavior. |
| crate `no_std` CI gate | ✓ | `cargo build -p kittens-render --target thumbv7em-none-eabi --release` in CI | Host/std tests are explicitly not evidence for this row; no separate negative fixture is needed for a target-build gate. |
| external `no_std` consumer/link | ✓ | The downstream `kittens-render-no-std-fixture` starts a transfer only through the target-supplied region, reconciles its mandatory settlement through the sweep, and links for `thumbv7em-none-eabi --release` in CI | Building only the profile rlib is the explicit non-control; adapter honesty remains the separately published sealing obligation. |
| conditional `InFlight` pin boundary | ✓ | `tests/ui/in_flight_is_conditionally_unpin.rs` and `tests/ui/in_flight_spare_is_conditionally_unpin.rs` reject `!Unpin` transfer/spare values | `tests/ui-pass/in_flight_unpin_when_parts_are_unpin.rs` proves `!Unpin` associated transport/buffer types add no bound. |
| panel-geometry admission | ✓ | Private-construction UI failures reject bypassing the admission type | `tests/ui-pass/custom_unvalidated_panel.rs` publishes the loudly named arbitrary-panel escape. |
| `TouchReader` untorn-snapshot contract | **documentation** | A future reviewed integration must discharge the prose obligation with one physical transaction | `tests/ui-pass/touch_reader_untorn_is_prose_only.rs` proves arbitrary multi-transaction implementations compile. |
| canonical host lifecycle | ✓ | CI runs `cargo run -p kittens-render --example host_sweep`, which drives demand → sweep → target-driven start → mandatory per-stripe settlement → written demand settlement over the admitted board geometry | `cargo test --all-targets` merely builds an example as a zero-test harness and is explicitly not the execution control. Failure paths are covered by the named runtime rows above. |
| board HIL (silicon wake delivery, TE, latency) | **GATED** | Hardware arrival + Xtensa gate; SPEC sections 7 and 9, `K2R0A-LOG.md` open item 2 | Host wake traces are explicit non-controls for silicon interrupt delivery, TE behavior, and measured latency. |
| `OwnedTransfer` sealing | **GATED** | Pre-freeze obligation (SPEC 5.2); lands with the freeze decision, not before | The test models, external fixture, and `dishonest_starter_ignores_region.rs` prove arbitrary implementations still compile during the experiment. |

Silent caps rule (root AGENTS.md): nothing above is claimed beyond its
status column; OPEN and GATED rows are the honest remainder of the slice.
